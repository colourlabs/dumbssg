# dumbssg

a static site generator built in C++.

## build

1. install dependencies (fedora / DNF distros)
```sh
sudo dnf install inja-devel yaml-cpp-devel md4c-devel cmake
```

2. make build folder and build
```sh
mkdir build
cd build && cmake .. 
make
```

4. ????

3. profit
```sh
./dumbssg
```